import React, { useState } from 'react'

const Teams = () => {
  const [Teams, setTeams] = useState([
    { teamName: 'test', members: ['Ternekaise, w6, mäk'] },
  ])
  return <div></div>
}

export default Teams
